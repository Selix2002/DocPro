[project]
name = "docpro"
version = "1.0.4"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "hupper>=1.12.1",
    "pyinstaller>=6.11",
]

[tool.uv.workspace]
members = [
    "backend",
    "frontend",
]
