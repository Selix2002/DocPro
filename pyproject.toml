[project]
name = "docpro"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "hupper>=1.12.1",
]

[tool.uv.workspace]
members = [
    "backend",
    "frontend",
]
